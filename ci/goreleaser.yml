# Generated by: tyk-ci/wf-gen
# Generated on: Thu Sep 23 14:04:36 UTC 2021

# Generation commands:
# ./pr.zsh -repos tyk-sync -title releng: latest releng -branch releng/updates
# m4 -E -DxREPO=tyk-sync


# Check the documentation at http://goreleaser.com
# This project needs CGO_ENABLED=1 and the cross-compiler toolchains for
# - arm64
# - macOS (only 10.15 is supported)
# - amd64


builds:
  - id: std
    ldflags:
      - -X github.com/TykTechnologies/tyk-sync/cmd.VERSION={{.Version}}
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    env:
      - CGO_ENABLED=0


dockers:
- ids:
    - std
  image_templates:
    - "tykio/tyk-sync:{{ .Tag }}-amd64"
    - "docker.tyk.io/tyk-sync/tyk-sync:{{ .Tag }}"
  build_flag_templates:
    - "--platform=linux/amd64"
    - "--label=org.opencontainers.image.created={{.Date}}"
    - "--label=org.opencontainers.image.title={{.ProjectName}}"
    - "--label=org.opencontainers.image.revision={{.FullCommit}}"
    - "--label=org.opencontainers.image.version={{.Version}}"
  use: buildx
  goarch: amd64
  goos: linux
  dockerfile: ci/Dockerfile.std
  extra_files:
    - "README.md"

# Build tykio/xDH_REPO, cloudsmith/xCOMPATIBILITY_NAME (arm64)
- ids:
    - std
  image_templates:
    - "tykio/tyk-sync:{{ .Tag }}-arm64"
    - "docker.tyk.io/tyk-sync/tyk-sync:{{ .Tag }}-arm64"
  build_flag_templates:
    - "--platform=linux/arm64"
    - "--label=org.opencontainers.image.created={{.Date}}"
    - "--label=org.opencontainers.image.title={{.ProjectName}}-arm64"
    - "--label=org.opencontainers.image.revision={{.FullCommit}}"
    - "--label=org.opencontainers.image.version={{.Version}}"
  use: buildx
  goarch: arm64
  goos: linux
  dockerfile: ci/Dockerfile.std
  extra_files:
    - "README.md"



docker_manifests:
  - name_template: tykio/tyk-sync:{{ .Tag }}
    image_templates:
    - tykio/tyk-sync:{{ .Tag }}-amd64
    - tykio/tyk-sync:{{ .Tag }}-arm64
  - name_template: tykio/tyk-sync:v{{ .Major }}.{{ .Minor }}{{.Prerelease}}
    image_templates:
    - tykio/tyk-sync:{{ .Tag }}-amd64
    - tykio/tyk-sync:{{ .Tag }}-arm64


nfpms:
  - id: std
    vendor: "Tyk Technologies Ltd"
    homepage: "https://tyk.io"
    maintainer: "Tyk <info@tyk.io>"
    description: |-
      Tyk Sync is a command line tool and library to manage and 
      synchronise a Tyk installation with your version control system (VCS).
    package_name: tyk-sync
    file_name_template: "{{ .ConventionalFileName }}"
    bindir: /opt/tyk-sync
    section: utils
    builds:
      - std
    formats:
      - deb
      - rpm
    contents:
      - src: "README.md"
        dst: "/opt/share/docs/tyk-sync/README.md"
    rpm:
      signature:
        key_file: tyk.io.signing.key
    deb:
      signature:
        key_file: tyk.io.signing.key
        type: origin
      lintian_overrides:
        - changelog-file-missing-in-native-package

publishers:
  - name: tyk-sync-unstable
    env:
      - PACKAGECLOUD_TOKEN={{ .Env.PACKAGECLOUD_TOKEN }}
      - REPO=tyk/tyk-sync-unstable
      - RPMVERS={{ .Env.RPMVERS }}
      - DEBVERS={{ .Env.DEBVERS }}
    cmd: /pc.sh {{ .ArtifactPath }}

checksum:
  disable: false

archives:
  - id: std
    builds:
      - std
    format_overrides:
      - goos: windows
        format: zip
    files:
      - README.md

changelog:
  sort: asc
  filters:
    exclude:
    - '^utils:'
    - (?i)typo
    - 'Merge (pull request|branch)'
    - '\[CI\]'
    - '(?i)\[Buddy\]'
    - 'cherry picked'
    - '^rel-eng:'
    - '^minor:'

release:
  github:
    owner: TykTechnologies
    name: tyk-sync
  prerelease: auto
  draft: true
  name_template: "{{.ProjectName}}-v{{.Version}}"

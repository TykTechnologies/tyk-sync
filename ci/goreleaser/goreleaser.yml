# Generated by: gromit policy
# Generated on: Mon Dec  4 13:54:34 UTC 2023

# Check the documentation at http://goreleaser.com
# This project needs CGO_ENABLED=1 and the cross-compiler toolchains for
# - arm64
# - amd64
builds:
  - id: std
    ldflags:
      - -X github.com/TykTechnologies/tyk-sync/main.Version={{.Version}}
      - -X github.com/TykTechnologies/tyk-sync/main.Commit={{.FullCommit}}
      - -X github.com/TykTechnologies/tyk-sync/main.BuildDate={{.Date}}
      - -X github.com/TykTechnologies/tyk-sync/main.BuiltBy=goreleaser
    goos:
      - linux
    goarch:
      - amd64
      - arm64
      - s390x
    binary: tyk-sync

dockers:
  # Build tykio/tyk-sync, docker.tyk.io/tyk-sync/tyk-sync (amd64)
  - ids:
      - std
    image_templates:
      - "tykio/tyk-sync:{{.Tag}}-amd64"
      - "docker.tyk.io/tyk-sync/tyk-sync:{{.Tag}}-amd64"
    build_flag_templates:
      - "--build-arg=PORTS=80"
      - "--platform=linux/amd64"
      - "--label=org.opencontainers.image.created={{.Date}}"
      - "--label=org.opencontainers.image.title={{.ProjectName}}"
      - "--label=org.opencontainers.image.revision={{.FullCommit}}"
      - "--label=org.opencontainers.image.version={{.Version}}"
    use: buildx
    goarch: amd64
    goos: linux
    dockerfile: ci/Dockerfile.std
    extra_files:
      - "ci/install/"
      - "README.md"
  # Build tykio/tyk-sync, docker.tyk.io/tyk-sync/tyk-sync (arm64)
  - ids:
      - std
    image_templates:
      - "tykio/tyk-sync:{{.Tag}}-arm64"
      - "docker.tyk.io/tyk-sync/tyk-sync:{{.Tag}}-arm64"
    build_flag_templates:
      - "--build-arg=PORTS=80"
      - "--platform=linux/arm64"
      - "--label=org.opencontainers.image.created={{.Date}}"
      - "--label=org.opencontainers.image.title={{.ProjectName}}"
      - "--label=org.opencontainers.image.revision={{.FullCommit}}"
      - "--label=org.opencontainers.image.version={{.Version}}"
    use: buildx
    goarch: arm64
    goos: linux
    dockerfile: ci/Dockerfile.std
    extra_files:
      - "ci/install/"
      - "README.md"
  
docker_manifests:
  - name_template: tykio/tyk-sync:{{ .Tag }}
    image_templates:
      - tykio/tyk-sync:{{ .Tag }}-amd64
      - tykio/tyk-sync:{{ .Tag }}-arm64
  - name_template: tykio/tyk-sync:v{{ .Major }}.{{ .Minor }}{{.Prerelease}}
    image_templates:
      - tykio/tyk-sync:{{ .Tag }}-amd64
      - tykio/tyk-sync:{{ .Tag }}-arm64
  - name_template: docker.tyk.io/tyk-sync/tyk-sync:{{ .Tag }}
    image_templates:
      - docker.tyk.io/tyk-sync/tyk-sync:{{ .Tag }}-amd64
      - docker.tyk.io/tyk-sync/tyk-sync:{{ .Tag }}-arm64

nfpms:
  - id: std
    vendor: "Tyk Technologies Ltd"
    homepage: "https://tyk.io"
    maintainer: "Tyk <info@tyk.io>"
    description: Tyk-Sync is a command line tool and library to manage and synchronise a Tyk installation with your version control system (VCS).
    package_name: tyk-sync
    file_name_template: "{{ .ConventionalFileName }}"
    builds:
      - std
    formats:
      - deb
      - rpm
    contents:
      - src: "README.md"
        dst: "/opt/share/docs/tyk-sync/README.md"
      - src: "ci/install/*"
        dst: "/opt/tyk-sync/install"
      - src: ci/install/inits/systemd/system/tyk-sync.service
        dst: /lib/systemd/system/tyk-sync.service
      - src: ci/install/inits/sysv/init.d/tyk-sync
        dst: /etc/init.d/tyk-sync
    scripts:
      preinstall: "ci/install/before_install.sh"
      postinstall: "ci/install/post_install.sh"
      postremove: "ci/install/post_remove.sh"
    bindir: "/opt/tyk-sync"
    rpm:
      scripts:
        posttrans: ci/install/post_trans.sh
      signature:
        key_file: tyk.io.signing.key
    deb:
      signature:
        key_file: tyk.io.signing.key
        type: origin

publishers:
  - name: tyk-sync-unstable
    env:
      - PACKAGECLOUD_TOKEN={{ .Env.PACKAGECLOUD_TOKEN }}
      - REPO=tyk/tyk-sync-unstable
      - RPMVERS={{ .Env.RPMVERS }}
      - DEBVERS={{ .Env.DEBVERS }}
    cmd: /pc.sh {{ .ArtifactPath }}


# This disables archives
archives:
  - format: binary
    allow_different_binary_count: true

checksum:
  disable: true

release:
  disable: true
  github:
    owner: TykTechnologies
    name: tyk-sync
  prerelease: auto
  draft: true
  name_template: "{{.ProjectName}}-v{{.Version}}"
